# Git LFS configuration for large model files

# Track binary model files with Git LFS
models/**/*.bin filter=lfs diff=lfs merge=lfs -text
models/**/*.safetensors filter=lfs diff=lfs merge=lfs -text
models/**/*.onnx filter=lfs diff=lfs merge=lfs -text

# Regular git for text files
models/**/*.json text
models/**/*.txt text
models/**/*.md text
